import { Product } from "@openpos/socket-contracts";
const AddProductScreen = ({ product }: { product: Product }) => {
  //TODO: Modify useScreen hook to accept props
  return <></>;
};

export default AddProductScreen;
